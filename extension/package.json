{
  "name": "twitter-bookmarks-extension",
  "version": "1.0.0",
  "description": "Chrome extension for collecting Twitter/X bookmarks",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && npm run copy-files",
    "copy-files": "node scripts/copy-files.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  }
}
